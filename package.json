{
  "name": "celestique-ai-recipes",
  "version": "3.0.0",
  "description": "Advanced AI Recipe Generator with Culinary Intelligence",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "npm run build:public",
    "build:public": "mkdir -p public/styles public/scripts && cp index.html public/ && cp styles/main.css public/styles/ && cp scripts/app.js public/scripts/",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.6.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}